/**
 * Default values for all markdown files in the project
 * This applies the redirect defaults to all .md files
 */
module.exports = {
  layout: "redirect.njk",
  redirect_url: "https://example.com",
  redirect_delay: 3000,
};
